<% if @computers.count > 0 %>
    $('#computers').html("<%= escape_javascript render(:partial => 'tblPC') %>");
    $('#computers').show();
    /*$('#pcKill').click( function setDown() { 
        if (confirm('Seguro que quiere dar de baja el equipo?'))
            alert('equipo eliminado');
        else
            alert('equipo no eliminado');
    } ); */
<% else %>
    $('#computers').hide();
<% end %>

<% if @printers.count > 0 %>
    $('#printers').html("<%= escape_javascript render(:partial => 'tblPrint') %>");
    $('#printers').show();
<% else %>
    $('#printers').hide();
<% end %>

<% if @copiers.count > 0 %>
    $('#copiers').html("<%= escape_javascript render(:partial => 'tblCopier') %>");
    $('#copiers').show();
<% else %>
    $('#copiers').hide();
<% end %>

<% if @powers.count > 0 %>
    $('#powers').html("<%= escape_javascript render(:partial => 'tblPower') %>");
    $('#powers').show();
<% else %>
    $('#powers').hide();
<% end %>